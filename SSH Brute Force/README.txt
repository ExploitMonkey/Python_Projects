This is an SSH brute force tool, only use this tool against authorized targets.
WHEN USING THIS TOOL MAKE SURE TO HAVE A PASSWORD FILE NAMED "ssh-passwords.txt" IN THE SAME LOCATION YOU ARE RUNNING THE SCRIPT FROM

NOTE: If you are trying to run this against yourself at 127.0.0.1 use the following command
CMD: sudo service ssh start
